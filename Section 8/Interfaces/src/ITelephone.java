public interface ITelephone {
//    Methods
//    for a class that implements this interface, it must implement these methods
     void powerOn();
     void dial(int phoneNumber);
     void answer();
     boolean callPhone(int phoneNumber);
     boolean isRinging();
}
